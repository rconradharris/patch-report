- Multi-project
    - Make data_directory a global setting, not part of project


    - Add config.get_for_project()

    - Make get_patch_series() and get_last_updated_at() take `patch_id`
      argument

    - refresh_patch_series() should take `patch_id` argument

    - add refresh_all_projects() function which uses refresh_patch_series()

    - bin/refesh should call refresh_all_projects()

    - Add new index page dashboard
        - Show number of patches for each project

- Move templates and static out of project directory and up one level
- Make RE_RM_LINK use redmine_url config
- Add Gerrit title/status to modal
- Add setup.py
